# OmniCompare 默认不对外索引。
#
# 原因：本项目未内置任何鉴权机制（见 README「注意事项」），所有访问者均可上传与删除内容。
# 允许搜索引擎收录会把「无鉴权的写入入口」暴露成公开可发现的攻击面。
# 若你已在反向代理层加好 Basic Auth / IP 白名单，确实希望被收录，再把 Disallow 改成 Allow。
User-agent: *
Disallow: /
