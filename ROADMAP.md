# OmniCompare 路线图（后期方向与任务规划）

> **这份文件回答「接下来做什么」。** 任何一轮开发开工前，先读本文档选题；做完一项就回来更新状态，并把实施细节与决策沉淀进 [docs/BLUEPRINT.md](./docs/BLUEPRINT.md)（决策 D 系列 + Step 实施表）、架构约束沉淀进 [PROJECT.md](./PROJECT.md)、踩坑沉淀进 [docs/LESSONS-LEARNED.md](./docs/LESSONS-LEARNED.md)。
>
> 与 BLUEPRINT 的分工：**BLUEPRINT 记录「已定的决策与已完成的实施」，本文档记录「未来要做的事」**。两者互为镜像：本文档条目完成后，从 BLUEPRINT 侧能查到它是怎么做的；BLUEPRINT 里的新决策，会反过来在本文档里开出新条目。

---

## 0. 使用与维护约定

- **状态标记**：`[ ]` 未开始 · `[~]` 进行中（注明当前进度） · `[x]` 已完成（注明日期与提交） · `[-]` 明确不做（必须写明原因，防止反复重新评估）
- **优先级定义**：
  - **P0** —— 阻塞产品价值的最短板，立即做
  - **P1** —— 下一阶段的主菜，做完 P0 就轮到它
  - **P2** —— 中期规划，有价值但不紧急
  - **P3** —— 远期设想，仅记录不定案
- **新想法入册流程**：先写进对应方向的「候选池」小节 → 想清楚动机与验收标准后升级为正式条目（分配编号）→ 动工。禁止没写进本文档就直接开做新功能。
- **开工前必读**：第 12 节「技术红线与已知约束」+ [docs/LESSONS-LEARNED.md](./docs/LESSONS-LEARNED.md) 铁律清单 + [PROJECT.md](./PROJECT.md) 对应章节。
- **每步验证惯例延续**：lint + src 范围 tsc + API 实测 + 对抗/冒烟脚本回归 + 浏览器端到端，全部绿才算完成（详见 PROJECT.md 测试章节）。

---

## 1. 现状基线（2026-08-30）

规划前先记住我们已经有什么。以下能力全部实现、有测试覆盖、已推 GitHub（CI 全绿）：

| 能力域 | 现状 |
|---|---|
| 内容类型 | 视频（MP4/MOV/WebM 等）· 单文件自包含 HTML · zip 资源包 HTML（根级 index.html + 资产白名单）· 图片（PNG/JPG/GIF/WebP/SVG/BMP/AVIF，SVG 双重沙箱） |
| 布局展示 | Auto/Manual 矩阵 · 1–12 位动态数量 · 拖拽排序 · 全局+单卡比例 · 模糊背景填充（letterboxFill） |
| 播放控制 | 批量播放/暂停/重开/循环/静音/速度（0.5–2×），仅作用于视频；HTML「刷新全部」 |
| 项目管理 | 多项目 + 库视图 · 草稿/归档/进行中 · Studio/Focus 双模式 · 暗/亮双主题 |
| 安全 | iframe sandbox（绝不 allow-same-origin）+ 服务端 CSP/Disposition 双保险 · zip-slip 严防 · 上传 MIME+扩展名双判 |
| 存储 | 文件系统 + JSON 清单（无数据库）· 进程内互斥锁 · 原子写 · 清单↔磁盘 1:1 不变量 |
| 工程 | GitHub Actions CI 全绿（install/lint/tsc/build）· 对抗测试 160 项 + 冒烟 38 项 · Docker 三件套 + 备份脚本已备 |

**一句话结论：产品功能面已完整覆盖「单机自用」，当前最大的缺口是 ① 还没有真正部署到生产环境跑起来（方向 A）；② 对比评估的产品深度还没挖（方向 E）。**

---

## 2. 总览

| 方向 | 主题 | 优先级 | 条目 |
|---|---|---|---|
| A | 上线与运维——让产品真正跑起来 | **P0** | A1–A6 |
| B | 内容类型扩展 | P1 | B1–B4 + 候选池 |
| C | 展示与播放增强 | P1–P2 | C1–C6 + 候选池 |
| D | 数据管理与规模化 | P1–P2 | D1–D5 + 候选池 |
| E | 对比评估能力（产品灵魂深化） | P2 | E1–E4 |
| F | 多用户与分享 | P2–P3 | F1–F4 |
| G | 工程与体验增强 | P2 | G1–G7 |
| H | 远期设想（不定案） | P3 | H1–H4 |

---

## 3. 方向 A：上线与运维（P0，让产品真正跑起来）

> **为什么是 P0**：Docker 三件套、备份脚本、CI 全部就绪却从未真正部署。产品现在只存在于开发机上——一次磁盘故障就全没了。部署上线是当前投入产出比最高的事，也是后续一切方向（分享、多用户、评估协作）的前置条件。

### [ ] A1 真实服务器部署

- **动机**：Dockerfile / docker-compose / .dockerignore 已验证构建但没部署过；「换设备不丢」的核心卖点需要一个真实在线的服务端。
- **任务**：
  1. 确定部署目标（云服务器 / 家内 NAS / 内网主机，见第 13 节待拍板项）
  2. 服务器安装 Docker + Compose，`git clone` → `docker compose up -d --build`
  3. 验证数据卷挂载（宿主机 `./data`）、健康检查、容器自重启策略（`restart: unless-stopped`）
  4. 用真实域名或 IP 访问跑一遍完整流程（建项目→上传三类内容→重启容器→数据还在）
- **验收**：容器重启后数据无损；`/api/videos` 健康检查 200；宿主机能看到 `data/` 目录并可备份。
- **依赖**：无。**风险**：服务器架构差异（arm64 需重建镜像）；家庭宽带无公网 IP 需内网穿透（候选池：frp / Tailscale）。

### [ ] A2 访问控制

- **动机**：本项目**没有任何内置鉴权**，公网裸奔等于把数据与算力向所有人开放；这是上线后的第一号安全问题。
- **任务**（部署期临时方案，产品化方案见 F2）：
  1. 反向代理层（Nginx / Caddy）加 Basic Auth，保护除健康检查外的全部路由
  2. 或 IP 白名单 / Tailscale 内网限定（个人使用首选，零维护成本）
  3. 上传与删除类 API 建议在反代层限速防滥用
- **验收**：无凭据访问任意页面/API 返回 401；白名单外 IP 不可达。
- **注意**：反代 Basic Auth 对 `<video>` Range 请求与 `/api/bundles` 子路径同样生效，需实测流媒体播放不被鉴权打断（浏览器会带凭据重试，确认 206 响应正常）。

### [ ] A3 HTTPS 与域名

- **动机**：外链 CDN 资源（AI 页面常用 Tailwind/字体 CDN）在 http 页面下部分被拦；剪贴板、通知等 Web API 也要求 https；更关键的是传输安全。
- **任务**：Caddy（自动签证书，首选）或 Nginx + certbot 配置；强制 http→https 跳转；`HSTS` 头。
- **验收**：浏览器地址栏无「不安全」标记；带外链 CDN 的 HTML 内容卡完整渲染。

### [ ] A4 备份上 crontab + 恢复演练

- **动机**：`scripts/backup-data.sh`（tar.gz + 滚动保留 14 份）已实测但从未定时跑过；没有恢复演练的备份等于没有备份。
- **任务**：crontab 每日凌晨备份 → 异地副本（rclone 到对象存储或另一台机器）→ **真实演练一次**「删掉 data/ → 从备份恢复 → 数据无损」。
- **验收**：连续 3 天自动产出备份包；恢复演练通过并记录到 worklog。

### [ ] A5 健康监控与告警

- **动机**：服务挂了/磁盘满了没人知道。
- **任务**（保持轻量，不引入重型监控栈）：uptime-kuma 或 cron 探测 `/api/videos` + 磁盘水位脚本（`data/` 所在分区 >80% 告警）→ 推送通知（邮件/Server 酱/Telegram 任选）。
- **验收**：人为停容器 10 分钟内收到告警。

### [ ] A6 版本升级流程

- **动机**：上线后每次更新不能再靠「重跑一遍部署」的临时动作。
- **任务**：固化升级 SOP 到 PROJECT.md：`git pull` → `docker compose build` → `up -d` → 启动日志确认清单迁移无告警 → 冒烟脚本指向生产跑一遍。回滚方案：镜像 tag 化 + data/ 备份。
- **验收**：一次真实升级演练（含一次故意回滚）全程 ≤10 分钟且数据无损。

---

## 4. 方向 B：内容类型扩展（P1）

> **为什么值得做**：产品定位是「多内容并行对比」，支持的产出类型越广，适用场景越大。`kind` 判别联合的扩展面已在图片类型上验证过（新增一个 Asset 分支 + 一个 Panel 组件，不动骨架），每加一种类型的边际成本是可控的。数据模型 `ContentKind` 预留了 `markdown | pdf`。

### [ ] B1 音频类型（kind=audio）

- **动机**：TTS/语音合成模型对比是 AI 产出的重要类别（配音、音效、播客），与视频同属「带时间轴」的内容。
- **任务**：
  1. 服务端：上传校验加音频 MIME/扩展名（mp3/wav/ogg/m4a/flac），限额对齐视频
  2. 卡片 Panel：`<audio controls>` + 自绘波形或简洁封面（遵守 D7 空状态朴素原则）
  3. 批量控制语义：全部播放/暂停/静音作用于音频（与视频同组还是独立成组，动手前定案并记入 BLUEPRINT）
  4. 顶栏角标与库视图计数加入第三类
- **验收**：三类混合项目的上传/播放/排序/替换/删除/清理全链路 + 对抗脚本新增音频专项。
- **依赖**：无。**风险**：批量播放的「视频组/音视频混合组」语义需要定案（建议：合并进同一播放组，语义最简单）。

### [ ] B2 PDF 文档

- **动机**：报告、论文、设计稿导出的通用格式；对比「不同模型生成的文档」是真实场景。
- **任务**：iframe 直接嵌浏览器原生 PDF 查看器最省力（但 sandbox 与原生查看器兼容性要验证）；备选 pdf.js 自绘（可控性强、工作量大 3–5 倍）。先做 iframe 原生方案验证，不通再上 pdf.js。
- **验收**：多页 PDF 卡内翻页正常、比例框约束生效、无脚本执行风险（PDF 含 JS 的沙箱行为要测）。
- **风险**：iframe sandbox 下 Chrome/Firefox 原生 PDF 查看器行为不一致——这是先验证再定案的原因。

### [ ] B3 Markdown 卡片

- **动机**：LLM 文本输出的第一格式就是 Markdown（对话、代码、推理过程），文本对比是「对比」的题中之义。
- **任务**：`kind=markdown`；上传 .md 或卡内直接编辑（卡内编辑体验更好，但涉及输入框与拖拽/播放快捷键冲突）；渲染用 react-markdown（已是依赖）+ 代码高亮（react-syntax-highlighter 已是依赖）；样式遵守双主题语义色。
- **验收**：双主题渲染正确；长文滚动；代码块横向滚动不撑破比例框。
- **依赖**：无强依赖，建议排在 B1/B2 之后（文本类需要先想清楚「编辑态 vs 展示态」交互）。

### [ ] B4 外链 URL 内容（不落盘）

- **动机**：很多 AI 产出直接给在线链接（部署好的网页 demo、托管视频）；强制下载再上传很反人类。
- **任务**：`kind=url`，清单只存 URL 不存文件；iframe 渲染走与 html 相同的 sandbox；**安全评估先行**——嵌入第三方页面的 X-Frame-Options/CSP frame-ancestors 拒绝场景要优雅降级为「打开外链」按钮。
- **验收**：可嵌入/不可嵌入两种外链的表现都符合预期；清单校验拒绝 file:// 与内网地址（防 SSRF 与内网穿透探针）。
- **风险**：安全面比本地文件复杂（内网地址黑名单、redirect 链路）；建议放在 A2 访问控制之后做。

### 候选池（想法先记在这里，讨论后再升级）

- **Lottie/矢量动画**（.json 动画文件，AI 动效产出）——lottie-web 体积小，可挂在 image 之下或独立 kind
- **Office 文档（docx/pptx/xlsx）**——渲染成本高（需 mammoth/sheetjs 转换），建议先支持「PDF 导出后上传」并观察真实需求
- **视频直链流（HLS/m3u8）**——需要 hls.js，场景偏「播放」而非「对比」，优先级靠后

---

## 5. 方向 C：展示与播放增强（P1–P2）

> **为什么值得做**：核心用户旅程是「盯着矩阵看」，展示环节每打磨一分，对比效率就高一分。本方向条目普遍较小，适合与方向 B 的大条目穿插着做。

### [ ] C1 轮播/巡页模式

- **动机**：数字标牌/展厅大屏场景——内容多于同屏位数时自动轮播翻页；也是「录屏 demo」的常用姿势。
- **任务**：全局轮播开关（显示菜单）+ 间隔设置（10s/30s/60s）；按 order 分页翻动矩阵或单卡放大轮换（两种形态动手前定案）；Focus 模式下可用。
- **验收**：轮播启停/间隔生效、翻页不重载视频（复用「模式切换不丢状态」的实现套路）。

### [ ] C2 单卡放映

- **动机**：对比中经常需要「单独放大某个结果细看」——现有最大化是卡片级放大，缺一个真正的全屏放映态（隐藏其余内容与 UI、Esc 退出、←/→ 切换上一张/下一张）。
- **任务**：放映态组件 + 键盘导航 + 对视频/HTML/图片三类分别生效。
- **验收**：放映中视频不中断；HTML 保留运行状态（同 iframe 复用而非重挂载）或明确重挂载也可接受（动手前定案）。

### [ ] C3 HTML 定时自动刷新

- **动机**：监控看板类 HTML（时钟、天气、数据看板——测试项目里正好有现成样例）需要周期性回到初始态或拉新数据。
- **任务**：项目级设置 `htmlAutoRefreshSeconds`（0=关闭）；实现上复用「刷新全部」的 iframe 重载逻辑。
- **验收**：设置往返校验；刷新时无白屏闪烁（先淡出再重载或双缓冲，实测体验定方案）。

### [ ] C4 播放语义补全：单卡音量与全局速度扩展

- **动机**：全局静音之外，用户常需要「只听某个视频」；速度档位（0.5/1/1.25/1.5/2）之外偶尔要 0.75/3×。
- **任务**：激活态卡片加独立音量滑杆（不改全局 settings，或加 per-item 音量字段——动手前定案）；速度档位补全。
- **验收**：单卡音量与全局静音的叠加语义明确（建议：任一静音即静音）。

### [ ] C5 自定义比例 UI 补全

- **动机**：BLUEPRINT §13 已定义 `custom` 比例，但 UI 一直回落 16:9 容器——这是已知的功能欠账（w/h 输入弹窗从未实现）。
- **任务**：比例菜单加「自定义」弹窗（w/h 数字输入 + 常用预设 4:3/21:9/2.35:1）；服务端校验 customRatio 合法范围。
- **验收**：custom 生效于卡片框且 object-contain 铁律不破；对抗脚本加 custom 校验项。

### [ ] C6 外链资源开关（D13 设置入口的启用触发器）

- **动机**：BLUEPRINT §11 遗留项——外链 CDN 默认允许，但部署到公网后（方向 A），「禁用外链」成为真实需求（防泄漏内网 referer/防恶意脚本加载）；同时这是第一个「真全局设置」，触发 D13 侧栏设置入口的启用决策。
- **任务**：项目级或全局级开关（动手前定案，建议全局）；实现走服务端响应头 CSP（对 bundles/files 路由按配置注入 `CSP` 限制 connect-src/img-src 等）；侧栏「设置」入口按 D13 约定启用并承接此项。
- **验收**：开关开启后带外链的 HTML 内容卡外链资源全部加载失败且不报错白屏；关闭后恢复。

### 候选池

- 时钟/天气/日期小组件卡（kind=widget，免上传的内置内容）
- 字幕 overlay（视频卡叠加 SRT，配合对比讲解）
- 卡片冻结截图（把某一帧钉在卡上做「前后对照」）

---

## 6. 方向 D：数据管理与规模化（P1–P2）

> **为什么值得做**：上传的东西越来越多之后，「找回来、搬走、不撑爆」会成为真实痛点。BLUEPRINT 第二阶段清单里的「资产库」仍未落地。

### [ ] D1 资产库（跨项目素材复用）

- **动机**：同一批测试视频往往要在多个对比项目里反复用；每次重新上传既占存储又费操作。BLUEPRINT §18 第二阶段遗留项。
- **任务**：
  1. 引入「资产」层：文件物理上存一份（`data/library/[hash]/`），项目 item 引用资产 id
  2. 库视图加「素材库」页签：浏览/搜索/预览/删除（引用计数，被引用的资产删除需确认）
  3. 上传时按内容 hash 去重（同文件秒传）
- **验收**：两个项目引用同一资产、物理只存一份；删除引用项目后资产仍在库。
- **依赖**：建议在 A1 部署后做（涉及存储结构迁移，部署前做会加大首次部署的变数）。
- **风险**：这是数据模型层最大的重构（item.file → item.assetId），清单 schema 要升 v3 并写迁移脚本——遵循 v1→v2 迁移的幂等与备份惯例。

### [ ] D2 项目导入/导出

- **动机**：换服务器迁移、把对比成果分享给同事、离线归档。
- **任务**：导出 = manifest + files 打包 zip（含 bundle 目录）；导入 = 解包校验（复用 zip 包的安全校验逻辑：路径归一化、扩展名白名单、限额）+ id 冲突处理（重新生成项目 id）。
- **验收**：A 服务器导出 → B 服务器导入 → 全部内容可播放；恶意 zip（zip-slip/超限）被拒。
- **依赖**：无（可与 D1 独立）。

### [ ] D3 存储用量与配额

- **动机**：磁盘是自部署最常爆的资源；用户需要知道「谁吃的空间」。
- **任务**：库视图显示每项目/总用量（du 或清单 size 累加）；设置项：单项目上限/总量告警线；超限的报错文案要友好（继承现有上传报错风格）。
- **验收**：用量数字与磁盘实文件一致（对抗脚本校验）；超限上传被拒且不影响已有内容。

### [ ] D4 缩略图与封面

- **动机**：库视图和卡片目前只有文字与类型图标；视频/网页缺一眼可辨的封面。
- **任务**：视频首帧截取（服务端 ffmpeg 或前端 canvas 抓帧上传）；HTML 截图较难（需无头浏览器，权衡后可能只做站点 favicon/首屏截图跳过）——建议先只做视频封面。
- **验收**：封面生成不阻塞上传主链路；封面文件纳入清理不变量（删内容连带删封面）。

### [ ] D5 大文件与分片上传

- **动机**：当前上传走单请求 FormData，长视频（>500MB）易超时；Next.js Route Handler 的 body 限制也要绕。
- **任务**：分片上传（切片 + 序号 + 合并）或直接流式写盘；断点续传候选。
- **验收**：1GB 文件上传成功且不阻塞其他项目操作；中途断网重试不产生脏文件。
- **依赖**：仅在大文件真实成为痛点后做（YAGNI，先记录）。

### 候选池

- 回收站（删除软化，30 天可恢复）——对「误删对比成果」的保险
- 项目模板（新建时预置矩阵与设置）
- 全局搜索（跨项目搜标题/文件名）

---

## 7. 方向 E：对比评估能力（P2，产品灵魂深化）

> **为什么单列一个方向**：产品名叫 OmniCompare，但目前只有「并排放着看」，还没有「评」。BLUEPRINT §18 把「评分/标注（benchmark）、对比报告导出、API 自动抓取模型输出」列为后续扩展——这是产品从「展示工具」走向「评估工作台」的关键一步，也是同类产品最有差异化价值的部分。

### [ ] E1 评分与标注（benchmark 记分卡）

- **动机**：对比的终点是结论——「哪个模型产得好」。没有结构化的记录，结论只存在于脑子里。
- **任务**：
  1. item 级：评分（如 1–5 星或 10 分制）、标签（胜/负/平、优点/缺点标记）、文字备注
  2. 展示：卡片角标显示分数；库视图/导出时汇总
  3. 数据模型：item 加 `evaluation` 字段（schema v3 或独立 evaluation.json，动手前定案——倾向后者，保持内容清单纯净）
- **验收**：评分持久化、随导出走；空状态遵守 D7（朴素文字）。

### [ ] E2 对比报告导出

- **动机**：评审会有「看完要留档」的需求；BLUEPRINT §18 遗留项「导出对比快照」。
- **任务**：
  1. 轻量版：整个矩阵截屏导出 PNG（前端 html-to-image 或服务端 Playwright 截图）
  2. 完整版：Markdown/PDF 报告（项目名 + 矩阵截图 + 每卡标题/评分/备注，依赖 E1）
- **验收**：双主题下截图色彩正确；报告含全部 item 元数据。
- **依赖**：完整版依赖 E1。

### [ ] E3 轮次管理（多轮迭代对比）

- **动机**：真实工作流是「同一 prompt 喂多个模型 → 改 prompt → 再喂一轮」——目前只能建多个项目手工对应，轮次间无法快速切换对照。
- **任务**：项目内「轮次」分组或「轮次链」（项目 A 的 round1/round2）；轮次切换器 + 跨轮次并排对照模式（本轮 vs 上轮同模型位对齐）。
- **验收**：轮次切换不丢内容；对齐对照时 order 映射正确。
- **风险**：数据模型改动中等；建议在 E1 落地后做（评分按轮次记录才有意义）。

### [ ] E4 模型输出 API 自动回填

- **动机**：BLUEPRINT §18 遗留项——手动「跑模型→下载→上传」是重复劳动；直接在应用里输入 prompt、勾选模型、自动拉取产出回填矩阵。
- **任务**：
  1. 服务端任务队列：接各家 API（视频生成/图像生成/LLM）异步拉结果
  2. 卡片显示「生成中」状态 → 完成自动落位
  3. 密钥管理（服务端环境变量，绝不入库 manifest）
- **验收**：至少打通一家真实 API 全链路；失败重试与超时处理完备。
- **依赖**：**强烈建议在 A2 访问控制之后**（这个功能让「公网裸奔」的代价从「别人看你数据」升级为「别人烧你的 API key」）。
- **风险**：各家 API 形态不一，先做适配层抽象；这是本路线图中工作量最大的条目之一，值得独立开一轮。

---

## 8. 方向 F：多用户与分享（P2–P3）

> **定位**：A2 解决的是「上线期的门锁」，本方向是产品化的「门与钥匙」。单用户自用阶段不必急，但一旦出现第二个人要用，就从这里取方案。

### [ ] F1 只读分享链接

- **动机**：最低成本的协作——把某个对比项目生成带 token 的只读链接发给同事，对方浏览器直接看矩阵，不能改。
- **任务**：分享 token（项目级，可撤销）；只读视图复用 Focus 模式壳（天然干净）；分享页无管理 UI、无写操作 API 权限。
- **验收**：未持 token 不可访问；被撤销 token 立即失效；只读视角下写 API 全部 403。

### [ ] F2 多用户与访问码

- **动机**：小团队共用一台服务器。
- **任务**：轻量方案先行——全局访问码（环境变量，登录一次存 cookie）；进阶方案——多用户账号 + 项目归属（数据模型要加 user 维度，工程量大，需独立评估）。
- **依赖**：可与 D13 的「设置入口启用」联动（鉴权配置进设置页）。

### [ ] F3 云端账号与云项目

- **动机**：BLUEPRINT §18 远期项——数据从「自管文件系统」走向云存储。**在自部署场景下此项性价比低**（D2 导入导出已覆盖迁移需求），仅当转向 SaaS 时重启。

### [ ] F4 操作审计

- **动机**：多人使用后「谁删了我的项目」需要答案。追加写 `data/audit.log`（时间/动作/对象/来源 IP）即可，不必上数据库。
- **依赖**：F2 之后才有意义。

---

## 9. 方向 G：工程与体验增强（P2）

> **定位**：不直接产出用户可见功能，但决定项目能健康走多远。建议每轮大功能间隙捎带 1–2 项。

### [ ] G1 移动端适配

- **动机**：BLUEPRINT §18 遗留项「移动端编辑」。当前窄屏已有基本降级（矩阵收窄 2 列、顶栏横向滚动），但编辑体验（拖拽排序、双栏侧栏）未适配。
- **任务**：断点审查（<768px 全交互走点击而非拖拽）；顶栏/侧栏在移动端折叠方案；触摸长按排序或「上移/下移」按钮替代。
- **验收**：手机浏览器完整走通「建项目→上传→看→删」。

### [ ] G2 键盘快捷键

- **动机**：高频操作（播放/暂停、Focus 切换、刷新全部）鼠标路径太长。
- **任务**：Space 播放暂停、F 专注模式、R 刷新全部、1–9 定位卡片；快捷键面板（? 呼出）；避开输入框焦点态。
- **验收**：输入框聚焦时不触发全局快捷键；快捷键表入 README。

### [ ] G3 E2E 测试覆盖（Playwright）

- **动机**：现有测试面是 API 对抗 + 冒烟（160+38 项），UI 交互靠人工浏览器验证；核心交互回归成本高。
- **任务**：覆盖 Top 场景：建项目→上传三类→排序→设置往返→Focus 切换→删除清理；接进 CI（playwright 容器）。
- **验收**：CI 含 E2E job 且稳定绿（连跑 10 次 0 flaky）。

### [ ] G4 CI 增强

- **动机**：README 无徽章；无依赖缓存（每次全量装）；构建产物不存档。
- **任务**：README 加 CI 徽章；actions 缓存 bun（官方 cache 或 setup-bun 内置）；PR 上跑对抗脚本（当前只在本机跑）——把 160 项对抗搬进 CI 是测试资产化的关键一步。
- **验收**：CI 时长下降；PR 上对抗全绿可查。

### [ ] G5 性能压测与预算

- **动机**：12 卡 × 大视频同屏的真实负载从未量化。
- **任务**：构造极限项目（12×100MB 视频 + 12 HTML bundle）；度量首屏、内存、切换项目耗时；定性能预算并记录基准值，防后续功能悄悄劣化。
- **验收**：基准数据入 PROJECT.md；发现的问题单独立条目修。

### [ ] G6 双语界面（i18n）

- **动机**：README 已中英双版，UI 还是纯中文；英文用户（对比工具天然有海外受众）无法使用。
- **任务**：next-intl（已是依赖）接入；文案抽词表；语言跟随浏览器 + 手动切换。
- **验收**：双语言全界面无硬编码中文残留（用脚本扫描）。

### [ ] G7 可访问性（a11y）

- **动机**：键盘可达性、焦点管理、aria 标签、对比度——工程素养项，也是 G6 之外打开国际用户的另一块拼图。
- **任务**：axe 扫描 → 修高优先级问题；弹层焦点陷阱复核（Radix 已兜底大半）。
- **验收**：axe 无 critical 违规；纯键盘可完成核心流程。

### 候选池

- PWA（移动端「添加到主屏幕」看板）
- 桌面客户端壳（Tauri，投入大，先观察需求）

---

## 10. 方向 H：远期设想（P3，仅记录不定案）

- **H1 插件化类型系统**：内容类型注册表化（类型声明 + 上传校验 + Panel 组件打包成一个注册模块），新类型零侵入接入——等类型超过 5 种再评估是否值得。
- **H2 数字标牌模式**：多屏投放编排（一台浏览器全屏投一卡，多台设备组成超大矩阵，服务端同步翻页）——依赖 C1 轮播先落地。
- **H3 协同评审**：多人实时同看一个项目（WebSocket 广播播放控制与翻页）+ 逐卡评论流——依赖 F2 多用户先落地。
- **H4 SaaS 化**：托管多租户 + 订阅计费。与自部署定位冲突，除非产品方向重大调整，否则不做。

---

## 11. 排期建议（三步走）

> 节奏遵循 D6（分期慢慢加，每期可独立交付）。每期结束后回归全部测试面 + 更新本文档状态。

**第一步：上线（方向 A 全部）** —— 预计 1–2 个工作日
A1 部署 → A2 访问控制 → A3 HTTPS → A4 备份 crontab → A5 监控 → A6 升级 SOP。
完成标志：产品有一个稳定的公网/内网地址，数据有备份，挂了会告警。**这是当前唯一 P0。**

**第二步：类型与体验（B1 音频 → C5 自定义比例 UI → C3 HTML 自动刷新 → C2 单卡放映 → D2 导入导出 → C1 轮播）**
先补小欠账（C5/C3 是既有定义未实现的尾巴），再做新类型（B1），穿插数据出口（D2）。每条独立可交付，顺序可按当时兴趣调整。

**第三步：评估与规模（E1 评分 → E2 报告导出 → D1 资产库 → G3 E2E → B2 PDF → B3 Markdown）**
方向 E 让产品完成从「展示」到「评估」的跃迁；D1 是数据层大重构，安排在测试面充足的时期做。

> **如果不知道下一步做什么**：按本节顺序取第一个未完成条目；想跳到别的条目时，更新本文档并在 worklog 记一句原因即可——路线图服务于人，不是枷锁。

---

## 12. 技术红线与已知约束（做任何条目前必读）

以下约束来自 LESSONS-LEARNED 铁律与 BLUEPRINT 决策，**任何条目动工时都不允许突破**：

1. **沙箱红线**：iframe sandbox 绝不加 `allow-same-origin`；HTML/bundle 直链的 CSP/Disposition 安全响应头一个都不能少（BLUEPRINT §11）。
2. **数据不变量**：项目锁互斥（withManifestLock）、清单原子写、清单↔磁盘文件 1:1（删除内容必须连带清理文件/bundle 目录/封面等衍生物）、order 紧凑 0..n-1。
3. **data/ 是用户数据唯一存放地**：任何功能不得把用户内容写到 data/ 之外；备份只认 data/。
4. **模式切换不丢状态**：Studio/Focus/放映/轮播必须复用同一组件树仅换壳层，视频元素不得重建（历史教训：DOM 属性直写依赖元素不重建）。
5. **object-contain 铁律**：比例系统只控制卡片框，内容永不裁切；留白由底色/模糊层吸收。
6. **空状态朴素原则**（D7）：一切空位/空态用简单文字提示，禁止插画与花哨占位。
7. **Tailwind 扫描排除**：引入任何含类名的静态资产（原型/设计稿/示例 HTML）必须排除出 Tailwind 扫描源，否则构建 500（LESSONS-LEARNED 已记录）。
8. **并发留后手**：进程内锁够用；出现真实多实例/多机部署需求时，再引入 version 字段乐观锁（BLUEPRINT §19.6），不要提前建设。
9. **每步验证惯例**：lint + tsc + API 实测 + 对抗/冒烟回归 + 浏览器端到端；数据模型变更必须带幂等迁移脚本与迁移前备份。

---

## 13. 决策待定项（动工前需要拍板）

这些不是任务，而是对应条目动工前必须先回答的问题（回答后记入 BLUEPRINT 决策表）：

| # | 待定项 | 阻塞的条目 |
|---|---|---|
| Q1 | 部署目标环境：云服务器 / NAS / 内网主机？有无公网 IP 与域名？ | A1/A3 |
| Q2 | 访问控制选型：反代 Basic Auth / IP 白名单 / Tailscale？ | A2 |
| Q3 | 告警通道：邮件 / Server酱 / Telegram？ | A5 |
| Q4 | 音频与视频是否合并进同一播放组？ | B1 |
| Q5 | 轮播形态：矩阵整页翻 vs 单卡放大轮换？ | C1 |
| Q6 | 评分制式：星级 / 10 分制？evaluation 存 manifest 还是独立文件？ | E1 |
| Q7 | 外链开关作用域：全局 vs 按项目？ | C6 |

